{
  "name": "ts-x-unit",
  "repository": {
    "type": "git",
    "url": "git@github.com:FreeElephants/TSxUnit.git"
  },
  "description": "xUnit for TypeScript projects",
  "keywords": [
    "xUnit",
    "ts",
    "typescript"
  ],
  "author": "samizdam",
  "license": "BSD-2-Clause",
  "scripts": {
    "tsc": "tsc",
    "typings": "typings",
    "istanbul": "istanbul",
    "remap-istanbul": "remap-istanbul",
    "build": "echo 'Lucy in the sky with diamonds'",
    "pretest": "npm run build-launch-builder && node bin/launch-builder.js tests/ && tsc -p tests/",
    "build-launch-builder": "tsc @tsxunit-launch-builder.config",
    "test": "node tests/tests.js",
    "coverage": "istanbul cover tests/tests.js && remap-istanbul --input coverage/coverage.json --output coverage/html-report --type html"
  },
  "bin": {
    "launch-builder": "bin/launch-builder"
  },
  "main": "build/index.js",
  "typescript": {
    "definition": "build/index.d.ts"
  },
  "dependencies": {
    "istanbul": "^0.4.2",
    "jsdom": "^9.3.0",
    "jsmockito": "^1.0.5",
    "remap-istanbul": "^0.5.1",
    "sync-request": "^3.0.1",
    "typescript": "1.8.9",
    "typings": "1.3.1"
  },
  "version": "0.6.1",
  "devDependencies": {
    "tslint": "^3.7.1"
  }
}
